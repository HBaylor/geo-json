
/**
 * 返回公共的请求方法并挂载到vue实例上面
 *
 * @param {any} url 请求的地址
 * @param {any} params 请求的参数
 * @param {any} type 请求的类型  默认为 POST
 */

import Axios from 'axios'
import { url } from './url'
export const axios = function (url, params, type) {
  
}
